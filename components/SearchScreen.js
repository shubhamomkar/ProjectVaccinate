import React from 'react';
import { Text, View } from 'react-native';
import ScreenBackground from '../common/ScreenBackground';

const SearchScreen = () => {
    return (
        <ScreenBackground ola='ola'>
        <View><Text>SearchScreen</Text></View>
        </ScreenBackground>
    )
};

export default SearchScreen;